global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/price_api.rules.yml

scrape_configs:
  - job_name: price-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - 127.0.0.1:18743
