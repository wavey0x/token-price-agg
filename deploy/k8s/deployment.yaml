apiVersion: apps/v1
kind: Deployment
metadata:
  name: price-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: price-api
  template:
    metadata:
      labels:
        app: price-api
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "8000"
        price-api.io/probe-auth-note: "If API_KEY_AUTH_ENABLED=true, replace httpGet probes with authenticated exec probes."
    spec:
      containers:
        - name: app
          image: price-api:latest
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: price-api-secrets
            - configMapRef:
                name: price-api-config
          readinessProbe:
            httpGet:
              path: /v1/ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /v1/health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: price-api
spec:
  selector:
    app: price-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
